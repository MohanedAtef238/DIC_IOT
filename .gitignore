# Generated certs and secrets — never commit private keys or shared secrets
certs/*.key
certs/*.crt
certs/dtls_psk.json
node_modules
.npm

# Environment variables — contains mostly configuration values, not that secretive in this project's scope

# SQLite database written at runtime
*.db

# Python cache
__pycache__/
*.pyc
